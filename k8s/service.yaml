apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  selector:
    app: spring-boot-app
  ports:
    - protocol: TCP
      port: 80        # The port you will expose to localhost
      targetPort: 8080 # The port the Spring app listens on
  type: LoadBalancer  # Docker Desktop supports this to expose to localhost